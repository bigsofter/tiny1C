---
id: query-003
направление: core
категория: запросы
серьёзность: критично
платформы: [8.3, 8.5]
заголовок: "Псевдоним таблицы, совпадающий с именем табличной части, делает поле неоднозначным"
ключи: [Неоднозначное поле, Campo ambiguo, псевдоним, табличная часть, Представления, ЛЕВОЕ СОЕДИНЕНИЕ]
источник: "ошибка:maERP 2026-09-08, карточка номенклатуры, дополнительные реквизиты"
check: ["smoke-forms:форма не открывается", "manual:псевдонимы соединяемых таблиц сверить с именами табличных частей главной таблицы"]
---

# Псевдоним таблицы, совпадающий с именем табличной части, делает поле неоднозначным

## Симптом

Ошибка выполнения запроса «Неоднозначное поле "Представления.Ссылка"» (в локали клиента —
«Campo ambiguo»), хотя каждое поле в тексте квалифицировано псевдонимом. В EDT и при
CheckConfig запрос проходит: ошибка только на выполнении.

## Причина

К главной таблице `Реквизиты` присоединена таблица её же табличной части
`ПланВидовХарактеристик.Х.Представления` под псевдонимом `Представления`. Для платформы
`Представления.Ссылка` читается двояко: поле присоединённой таблицы или вложенная таблица
`Реквизиты.Представления` (табличная часть доступна как поле главной таблицы).

```bsl
// НЕПРАВИЛЬНО: псевдоним = имя табличной части главной таблицы
"... ИЗ ПланВидовХарактеристик.ДопРеквизиты КАК Реквизиты
|	ЛЕВОЕ СОЕДИНЕНИЕ ПланВидовХарактеристик.ДопРеквизиты.Представления КАК Представления
|	ПО Представления.Ссылка = Реквизиты.Ссылка"

// ПРАВИЛЬНО: псевдоним не совпадает ни с одним реквизитом главной таблицы
"...	ЛЕВОЕ СОЕДИНЕНИЕ ПланВидовХарактеристик.ДопРеквизиты.Представления КАК Заголовки
|	ПО Заголовки.Ссылка = Реквизиты.Ссылка"
```

## Как проверить

Для каждого соединения таблицы табличной части сверить псевдоним с именами табличных
частей и реквизитов главной таблицы. Прогоном ловится смоком открытия форм, если запрос
выполняется при открытии.

## История

maERP 2026-09-08: запрос `УправлениеСвойствами.СвойстваОбъекта` заголовков дополнительных
реквизитов по языку; карточка номенклатуры не открывалась. Псевдоним переименован в 2.0.14.22.
