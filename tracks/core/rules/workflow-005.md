---
id: workflow-005
направление: core
категория: процесс
серьёзность: важно
платформы: [8.3, 8.5]
заголовок: "Новую XML-конструкцию собирать по работающему образцу из этой же конфигурации"
ключи: [образец, УНФ, конструкция, XML, вслепую]
источник: "ошибка:maERP 2026-09-01, выдуманный тег width в UsualGroupExtInfo"
check: ["manual:частотный аудит тегов extInfo по всем формам конфигурации — разовым grep"]
---

# Новую XML-конструкцию собирать по работающему образцу из этой же конфигурации

## Правило

Прежде чем писать незнакомый узел (подменю, тумблер, панель, страница, картинка
закладки) - найти РАБОТАЮЩИЙ образец: сначала в своей конфигурации, затем в
выгрузке типовой конфигурации. Скопировать структуру образца, заменить имена.
Не найден образец - не собирать по памяти: сериализация EDT не очевидна
(порядок тегов, пространства значений, ссылки вида Enum.X.EnumValue.Y,
StdPicture против CommonPicture).

Форматы значений в choiceList/параметрах: `core:ReferenceValue`,
`core:NumberValue`, `core:StringValue`, `core:BooleanValue` - подсмотреть в
готовых формах.

## Механическая проверка

После сборки нового узла - частотный аудит тегов: собрать все теги своего extInfo
и сверить с теми же extInfo по ВСЕМ остальным формам конфигурации. Тег, который
не встречается больше нигде, - почти наверняка выдуманный.

## История

maERP 2026-08-31: конструкции, собранные по образцу (тумблер, кнопки, подменю),
вставали с первого раза; собранные по памяти (директива, id, пути) - падали.

maERP 2026-09-01: выдуманный тег width внутри UsualGroupExtInfo. EDT импорт
проглотила без ошибки, а КЛИЕНТ ПАДАЛ при построении формы списка - выдуманный
тег может быть не отвергнут на входе, а уронить платформу при рендеринге.
Найден частотным аудитом: единственное вхождение на всю конфигурацию.
